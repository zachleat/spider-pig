name: Node Unit Tests
on:
  push:
    branches-ignore:
      - "gh-pages"
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: ["ubuntu-latest", "macos-latest", "windows-latest"]
        node: ["20", "22", "24", "26"]
    name: Node.js ${{ matrix.node }} on ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Setup node
        uses: actions/setup-node@v6.4.0
        with:
          node-version: ${{ matrix.node }}
          # Remove this if you ever use CI publish
          cache: npm
      - run: npm ci
      - run: npm test
env:
  YARN_GPG: no
